# Clippy configuration for Pekobot
# Relaxed rules for practical development

# Documentation warnings - too pedantic for this codebase
avoid-breaking-exported-api = false

# Allow some patterns that are common in this codebase
allow-unwrap-in-tests = true
