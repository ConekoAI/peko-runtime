#!/bin/bash
#
# Pekobot Install from Source
# Usage: curl -fsSL https://raw.githubusercontent.com/coneko/pekobot/main/install-from-source.sh | bash
#

set -e

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

# Configuration
REPO_URL="https://github.com/coneko/pekobot.git"
INSTALL_DIR="${INSTALL_DIR:-/usr/local/bin}"
CONFIG_DIR="${CONFIG_DIR:-$HOME/.config/pekobot}"
DATA_DIR="${DATA_DIR:-$HOME/.local/share/pekobot}"
CLONE_DIR="${CLONE_DIR:-/tmp/pekobot-build}"

# Check dependencies
check_dependencies() {
    local deps=("git" "curl")
    local missing=()
    
    for dep in "${deps[@]}"; do
        if ! command -v "$dep" >/dev/null 2>&1; then
            missing+=("$dep")
        fi
    done
    
    if [ ${#missing[@]} -ne 0 ]; then
        echo -e "${RED}Missing dependencies: ${missing[*]}${NC}"
        if [[ " ${missing[*]} " =~ " rustc " ]]; then
            echo -e "${YELLOW}Install Rust: https://rustup.rs/${NC}"
        fi
        exit 1
    fi
    
    # Check for Rust
    if ! command -v cargo >/dev/null 2>&1; then
        echo -e "${RED}Rust/Cargo not found${NC}"
        echo -e "${YELLOW}Install Rust:${NC}"
        echo "  curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh"
        exit 1
    fi
    
    echo -e "${GREEN}✓ Dependencies OK${NC}"
}

# Clone repository
clone_repo() {
    echo -e "${BLUE}Cloning repository...${NC}"
    
    # Clean up old clone if exists
    if [ -d "$CLONE_DIR" ]; then
        echo -e "${YELLOW}  Removing old build directory...${NC}"
        rm -rf "$CLONE_DIR"
    fi
    
    git clone --depth 1 "$REPO_URL" "$CLONE_DIR"
    cd "$CLONE_DIR"
    
    echo -e "${GREEN}✓ Cloned to ${CLONE_DIR}${NC}"
}

# Build from source
build() {
    echo -e "${BLUE}Building Pekobot from source...${NC}"
    echo -e "${YELLOW}  This may take a few minutes...${NC}"
    
    cd "$CLONE_DIR"
    
    # Build release binary
    cargo build --release
    
    if [ ! -f "target/release/pekobot" ]; then
        echo -e "${RED}Build failed - binary not found${NC}"
        exit 1
    fi
    
    echo -e "${GREEN}✓ Build complete${NC}"
}

# Install binary
install_binary() {
    echo -e "${BLUE}Installing binary...${NC}"
    
    local binary_path="${CLONE_DIR}/target/release/pekobot"
    
    if [ -w "$INSTALL_DIR" ]; then
        cp "$binary_path" "${INSTALL_DIR}/pekobot"
        chmod +x "${INSTALL_DIR}/pekobot"
    else
        echo -e "${YELLOW}Requesting sudo access to install to ${INSTALL_DIR}${NC}"
        sudo cp "$binary_path" "${INSTALL_DIR}/pekobot"
        sudo chmod +x "${INSTALL_DIR}/pekobot"
    fi
    
    echo -e "${GREEN}✓ Installed to ${INSTALL_DIR}/pekobot${NC}"
}

# Setup directories
setup_directories() {
    echo -e "${BLUE}Setting up directories...${NC}"
    
    mkdir -p "$CONFIG_DIR"
    mkdir -p "$DATA_DIR"
    mkdir -p "$DATA_DIR/tools"
    mkdir -p "$DATA_DIR/workspaces"
    
    # Create default config
    if [ ! -f "${CONFIG_DIR}/config.toml" ]; then
        cat > "${CONFIG_DIR}/config.toml" <<EOF
# Pekobot Configuration
# Generated by install-from-source.sh on $(date -I)

[agent]
name = "default"
provider = "openai"
model = "gpt-4o-mini"

[memory]
type = "sqlite"
path = "${DATA_DIR}/memory.db"

[tools]
registry = "pekohub"
registry_url = "https://tools.coneko.ai"
auto_install = true

[daemon]
enabled = true
poll_interval = 15
EOF
        echo -e "${GREEN}✓ Config created at ${CONFIG_DIR}/config.toml${NC}"
    fi
}

# Install systemd service (Linux only)
install_systemd_service() {
    if [ "$(uname -s)" != "Linux" ]; then
        return 0
    fi
    
    if ! command -v systemctl >/dev/null 2>&1; then
        echo -e "${YELLOW}systemd not detected, skipping service installation${NC}"
        return 0
    fi
    
    echo -e "${BLUE}Installing systemd service...${NC}"
    
    local service_file="/etc/systemd/system/pekobot.service"
    
    if [ -w "/etc/systemd/system" ]; then
        cat > "$service_file" <<EOF
[Unit]
Description=Pekobot Agent Runtime
After=network-online.target

[Service]
Type=simple
User=%I
ExecStart=${INSTALL_DIR}/pekobot daemon start
ExecStop=${INSTALL_DIR}/pekobot daemon stop
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF
    else
        echo -e "${YELLOW}Requesting sudo for systemd service${NC}"
        sudo tee "$service_file" > /dev/null <<EOF
[Unit]
Description=Pekobot Agent Runtime
After=network-online.target

[Service]
Type=simple
User=%I
ExecStart=${INSTALL_DIR}/pekobot daemon start
ExecStop=${INSTALL_DIR}/pekobot daemon stop
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF
    fi
    
    sudo systemctl daemon-reload 2>/dev/null || true
    echo -e "${GREEN}✓ Systemd service installed${NC}"
    echo -e "${YELLOW}  Enable: sudo systemctl enable pekobot@\$USER${NC}"
    echo -e "${YELLOW}  Start:  sudo systemctl start pekobot@\$USER${NC}"
}

# Print post-install info
print_post_install() {
    echo ""
    echo -e "${GREEN}═══════════════════════════════════════════════════${NC}"
    echo -e "${GREEN}  Pekobot installed from source!${NC}"
    echo -e "${GREEN}═══════════════════════════════════════════════════${NC}"
    echo ""
    echo -e "${BLUE}Quick Start:${NC}"
    echo ""
    echo "  1. Set your API key:"
    echo "     export OPENAI_API_KEY='your-key-here'"
    echo ""
    echo "  2. Create an agent:"
    echo "     pekobot agent create my-agent"
    echo ""
    echo "  3. Run the agent:"
    echo "     pekobot agent start my-agent"
    echo ""
    echo -e "${BLUE}Development:${NC}"
    echo "  Source: ${CLONE_DIR}"
    echo "  Build:  cd ${CLONE_DIR} && cargo build --release"
    echo ""
    echo -e "${BLUE}Configuration:${NC}"
    echo "  Config: ${CONFIG_DIR}/config.toml"
    echo "  Data:   ${DATA_DIR}"
    echo ""
    echo -e "${YELLOW}To update later, just re-run this script${NC}"
}

# Clean up build directory
cleanup() {
    echo -e "${BLUE}Cleaning up...${NC}"
    rm -rf "$CLONE_DIR"
    echo -e "${GREEN}✓ Cleaned up ${CLONE_DIR}${NC}"
}

# Main flow
main() {
    echo -e "${BLUE}═══════════════════════════════════════════════════${NC}"
    echo -e "${BLUE}  Pekobot Installer (from source)${NC}"
    echo -e "${BLUE}  github.com/coneko/pekobot${NC}"
    echo -e "${BLUE}═══════════════════════════════════════════════════${NC}"
    echo ""
    
    check_dependencies
    clone_repo
    build
    install_binary
    setup_directories
    install_systemd_service
    cleanup
    print_post_install
}

# Handle flags
while [[ $# -gt 0 ]]; do
    case $1 in
        --keep-source)
            KEEP_SOURCE=1
            shift
            ;;
        --install-dir)
            INSTALL_DIR="$2"
            shift 2
            ;;
        --help|-h)
            echo "Usage: install-from-source.sh [OPTIONS]"
            echo ""
            echo "Options:"
            echo "  --keep-source       Keep the source directory after install"
            echo "  --install-dir DIR   Install to custom directory"
            echo "  --help, -h          Show this help"
            exit 0
            ;;
        *)
            echo "Unknown option: $1"
            exit 1
            ;;
    esac
done

main

# Keep source if requested
if [ "${KEEP_SOURCE:-0}" -eq 1 ]; then
    echo ""
    echo -e "${YELLOW}Source kept at: ${CLONE_DIR}${NC}"
fi
