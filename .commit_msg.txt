refactor: move sessions and workspaces to data_dir

Move sessions from config_dir to data_dir/sessions/{team}/{agent}/
Move workspaces from config_dir to data_dir/workspaces/{team}/{agent}/
Update PathResolver with new path methods and proper separation
Remove legacy path resolution code
Update all services to use PathResolver consistently

This follows XDG conventions:
- config_dir: Agent configurations (configs, credentials)
- data_dir: Sessions, workspaces, tools, runtime data
- cache_dir: Temporary files and caches
