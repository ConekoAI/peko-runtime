fix: update test files to match current API

- Fix session_management.rs:
  - Use SessionKeyContext instead of SessionContext for key derivation
  - Add missing peer_type and peer_id fields to SessionEntry structs
  
- Fix tool_wiring_test.rs:
  - Remove outdated test using non-existent AgentManager
  
Tests now compile and pass successfully.
