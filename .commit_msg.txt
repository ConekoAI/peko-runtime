fix(tests): cli_basics agent/team tests need daemon; config tests are offline

Root cause: agent create/list/show/remove/move and team create/list/show/
remove/move ALL route through the daemon via IPC. Only config commands
operate directly on the filesystem.

Changes:
- Add DaemonGuard + mock_llm_url() to all agent/team tests
- Mark agent/team tests with #[ignore] requiring MOCK_LLM_URL
- Keep config tests as always-run (no daemon needed)
- Fix config_init_creates_config_file: config init writes peko.toml
  (not config.toml) to the current directory
- Update Makefile test-cli-basics to use docker-up and --include-ignored
